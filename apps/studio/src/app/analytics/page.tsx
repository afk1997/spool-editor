"use client";

import { FutureScreen } from "@/components/spool/panels";

/* Analytics — Phase 4. 1:1 port of the demo's FutureScreen placeholder (07). */
export default function AnalyticsScreen() {
  return (
    <FutureScreen code="Analytics" phase="4" icon="chart" title="Analytics unavailable" desc="Spool does not collect or display performance analytics in this build. No platform metrics have been loaded." />
  );
}
