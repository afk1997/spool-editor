# Server-only engine origin. Keep the path at the origin root; do not append /api/v1.
SPOOL_ENGINE_URL=http://127.0.0.1:8899

# Required here whenever Flask is launched with TROVE_TOKEN. Use the same secret value.
# SPOOL_ENGINE_TOKEN=replace-with-the-engine-token
