flask>=3.0
# yt-dlp pinned to a released version: the master tarball made builds non-reproducible
# and shipped unreviewed upstream changes straight into the binary that processes
# user-supplied URLs. Bump deliberately (YouTube breakage is the usual trigger).
yt-dlp==2025.10.14
pytest>=8.0
pytest-timeout>=2.3
pywhispercpp>=1.2.0
psutil>=5.9

# --- Diarization (speaker labelling) ------------------------------------
# Now installed by default. Set TROVE_DIARIZATION=on at runtime to
# enable speaker detection on transcribe. PyTorch is pulled in
# transitively by resemblyzer (~1.3GB on Linux due to NVIDIA wheels).
resemblyzer>=0.1.4
silero-vad>=5.1
scikit-learn>=1.3
mcp>=1.0

# Face detection for per-shot face-tracking reframe (clip/face_track.py)
opencv-python-headless
