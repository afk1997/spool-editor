packages:
  - "apps/*"
  - "packages/*"

# pnpm 10 blocks dependency build scripts by default; these are the ones
# create-next-app opts out of building (native deps Next handles itself).
ignoredBuiltDependencies:
  - sharp
  - unrs-resolver
